{% block body %}
{{ form_start(form) }}
    {{ form_row(form.email) }}
    {{ form_row(form.password) }}
    <button class="btn">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}

{% endblock %}