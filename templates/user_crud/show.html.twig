{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/admin_crud.css') }}">
{% endblock %}

{% block body %}
    <h1 class="titre">User</h1>

    <table class="table">
        <tbody>
            <tr class="tableIndexShow">
                <th>Email</th>
            </tr>
            <tr class="tableDescriptionShow">
                <td>{{ user.email }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('user_crud/_delete_form.html.twig') }}
    <a href="{{ path('app_user_crud_edit', {'id': user.id}) }}" class="btnAction">Modifier</a>
    <a href="{{ path('app_user_crud_index') }}" class="btnAction">Retour</a>
{% endblock %}
